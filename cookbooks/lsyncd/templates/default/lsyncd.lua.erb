settings = {
     logfile    = "/var/log/lsyncd/lsyncd.log",
     statusFile = "/var/log/lsyncd/lsyncd-status.log",
     statusInterval = 20
            
}
 
<% @slaves.each do |server| %>
sync{
      default.rsyncssh,
      source="<%= @location %>",
      host="root@<%= server[:rackspace][:local_ipv4] %>",
      targetdir="<%= @location %>",
      rsyncOpts="-avz"
}
<% end %>
